SET @ROW_NUMBER:=-1;
SELECT ROUND(AVG(N.LAT_N),4) FROM 
/*INNER QUERY*/
(SELECT @ROW_NUMBER:=@ROW_NUMBER+1 AS ROWNUMBER,STATION.LAT_N FROM STATION ORDER BY STATION.LAT_N ASC )AS N
WHERE N.ROWNUMBER IN (FLOOR(@ROW_NUMBER/2),CEIL(@ROW_NUMBER/2));
